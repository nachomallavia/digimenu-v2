# Supabase (DIG-3)
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
# Server-only — never expose to the client
SUPABASE_SERVICE_ROLE_KEY=

# Owner cookie signing — deferred (DIG-13 Alpha uses session + DB only)
# DIGIMENU_OWNER_COOKIE_SECRET=

# Product CSV id signatures (DIG-21) — required for export/import
DIGIMENU_ID_HASH_SECRET=
