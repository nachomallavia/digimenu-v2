---
import Root from "./Root.astro";

interface Props {
	title?: string;
}

const { title = "DigiMenu — App" } = Astro.props;
---

<Root title={title}>
	<div class="min-h-dvh">
		<!-- Owner chrome (sidebar) lands in DIG-16 -->
		<slot />
	</div>
</Root>
